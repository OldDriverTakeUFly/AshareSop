# cardgen 金融信息卡片生成系统——落地与验收记录

日期:2026-08-28 | 分支:cardgen(82d8ec2..c347f49,9 任务 + 终审修复波) | 设计 spec:docs/superpowers/specs/2026-08-28-cardgen-design.md

## 一、系统概貌

`davis_analyzer/cardgen/`(11 模块,CLI `python -m davis_analyzer.cardgen {init|ingest|validate|build|status|enqueue}`),补齐「研报/数据 → 卡片」的内容生产与质量段,与 scripts/card_factory(渲染)、scripts/content_publisher(发稿)松耦合对接:

- **双层内容模型**:结构化数字字段 `$fact` 强引用 + 叙事文本数值 token 全量提取比对,每个数字必须命中 facts.json 事实清单(带研报章节锚点或 Tushare 查询指纹)。
- **四道机器闸**(validate):数字全量核对 / 合规(敏感词全文本+尾卡免责+来源标注+waivers 豁免)/ 完整性 / 事实自检。不过不准 build;build 再查过期(expires_at);enqueue 打印发稿命令前再查一次。
- **台账**:storage/database/content_cards.db(cards/revisions/validate_log),版本 bump 须 reason,facts/spec digest 变更强制走修订链。
- **测试**:cardgen 86 项 + 全仓 1248 项全绿(渲染集成用例真实跑 node/playwright)。

## 二、GPU四小龙重建验收(验收标准=spec §9)

- **55 条事实全部溯源核实**(四研报各 18/15/13/8 + 1 manual 结构计数,quote 逐条锚到原文行号);validate 退出码 0;新产物在 `docs/小红书卡片/GPU四小龙/output/`(6 PNG + RELEASE.json),顶层 2026-08-28 历史 PNG 未动。
- **系统首战抓到真实转写误差**:原卡03「Q1市占率首破52.3%」全仓库无出处,按沐曦研报 §4.6(L265 TrendForce 口径)改为「出货预计+83%」——数字溯源闸门防住的事故类型实证。
- **与历史 PNG 差异说明**:5 张差异 1.2%~4.8%(裁定类改写:日期统一 ISO、「从零到一」「断层一/二」、逐卡补「数据来源:」前缀、卡03 上述修正);卡04 差异 26.6% 系历史图出自手写 cards.html(彩色底轨/32px),重建走 build_cards.py 标准渲染器(灰底轨/30px),数值与条长比例逐项一致——非内容差异。
- 已知豁免:仅 1 条,卡04「非目标价」否定语境(compliance_waivers.json 登记)。

## 三、合并后跟进清单(终审 triage,均为小改)

| 项 | 位置 | 说明 |
|---|---|---|
| _display_selfcheck 尾零误报 | facts.py | value="17.360"+display"17.36亿"会误报(fail-closed 方向,烦而不险);改用 _plain 归一化匹配 |
| 3 条 quote 纯度 | GPU facts.json | cf_neg_high/low、moore_unlock_pct 混入登记者口径尾注(数字本体全对);收紧为纯原文 |
| builder print→stderr | builder.py:47 | 违反「库代码 loguru」约定,一行改 |
| 状态机回退守卫 | cli.py cmd_validate | validate 会把 rendered/queued 拉回 validated(真实台账已发生一次);加单调守卫 |
| 空 foot 逃逸 | compliance.py | 无 foot 的非尾卡不检查来源标注(fail-open 边界) |
| 单位字符集三处平行维护 | numbers.py | 年份掩码/mm/dd 掩码/_UNIT 各一份,新增单位须三处同步,否则两位数区间对新单位重新 fail-open |
| ingest 后 facts 顺序抖动 / enqueue 读原始 spec / 打印命令单引号 | cli.py | cosmetic |

M2(发布系统侧):RELEASE 契约定稿(images 为工程目录相对路径,已三方钉死;validate 字段含 failures 计数)——由 cardgen 侧交付,queue.py schedule 硬闸消费留 M2。

## 四、agent 使用纪律(同步沉淀于 davis_analyzer/AGENTS.md)

①卡片任何数字必须登记 facts.json 且带来源锚点,叙事观点必须能指回研报章节;②已 rendered 版本变更须 `--bump --reason`,过期(expires_at)卡片禁止 build/enqueue;③不得修改 scripts/card_factory 与 scripts/content_publisher(对接只读)。
